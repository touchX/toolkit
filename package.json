{
  "name": "sky-toolkit",
  "version": "1.0.0",
  "description": "Sky Toolkit",
  "main": "toolkit.scss",
  "scripts": {
    "test": "echo 'No tests to run'",
    "dist": "mkdir -p ./dist && cat ./toolkit.scss | node_modules/.bin/node-sass --include-path node_modules/ --output-style compressed > dist/toolkit.css"
  },
  "orion": {
    "default": {
      "stylesheet": "pages/assets/css/toolkit.css"
    }
  },
  "author": "Sky UK",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^3.4.2"
  },
  "dependencies": {
    "sky-toolkit-core": "0.5.1",
    "sky-toolkit-ui": "0.5.0"
  }
}
