machine:
  node:
    version: 5.5.0
  ruby:
    version: 2.3.0
deployment:
  release:
    branch: master
    commands:
      - npm run build 
      - npm run update-pages
      - bundle exec jekyll build
      - bundle exec rake deploy
